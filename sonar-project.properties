# === Identifiant du projet sur SonarQube ===
sonar.projectKey=taskmanager

# === Nom affiché dans SonarQube ===
sonar.projectName=TaskManager
sonar.projectVersion=1.0

# === Dossier contenant les sources ===
sonar.sources=backend

# === Langue principale utilisée ===
sonar.language=js

# === Fichiers ou dossiers à exclure de l'analyse ===
sonar.exclusions=**/node_modules/**,**/test/**,**/*.spec.js

# === Encodage des fichiers source ===
sonar.sourceEncoding=UTF-8

# === Clé d'analyse / token (fourni via Jenkins Credentials) ===
# sonar.login=squ_9a227988bb01a48e08fb0253cef85a013e82b665
# ⚠️ Ne pas décommenter ceci si Jenkins injecte le token via l'environnement
